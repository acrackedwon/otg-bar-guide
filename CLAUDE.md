# ON THE GROUND 직원 가이드 — 작업 규칙

## 다국어는 필수 규칙이다

**콘텐츠를 추가하거나 수정할 때는 반드시 한국어와 영어를 함께 작업한다.**

한쪽만 올리면 안 된다. 사장님이 한국어로만 내용을 주시더라도, 영어판을 같이 만들어
같은 커밋에 넣는다. 확인을 받을 필요 없이 기본 동작이다.

- `content/ko/<파일>.md` 와 `content/en/<파일>.md` 는 **항상 짝을 이룬다**
- **파일 이름과 `순서` 번호는 두 언어에서 동일해야 한다.** 이 둘로 같은 항목임을 판별한다
- 한쪽에만 파일이 있으면 사이트는 한국어로 대체 표시한다 — 동작은 하지만 미완성 상태다

영어는 직역하지 않는다. 외국인 직원이 실제로 읽고 바로 따라할 수 있는 문장으로 쓴다.

**독자는 한국인 직원과 외국인 직원(주로 유럽)이 섞여 있다.** 영어판은 보조 자료가 아니라
실제로 현장에서 쓰이는 문서다. 한국어판과 같은 수준으로 구체적이어야 하며, 한국 특유의
맥락(분리수거 구분, 매장 은어, 제품 약칭)은 영어판에서 풀어서 설명한다.

### 언어를 추가할 때

1. `lib/content.ts` 의 `LANGUAGES` 에 항목 추가
2. `src/main.ts` 의 `UI` 에 화면 문구 한 벌 추가
3. `content/<코드>/` 폴더를 만들고 기존 파일을 모두 번역

## 콘텐츠 파일 규칙

- 파일 하나 = 항목 하나. 이름은 `<순서번호>-<영문 슬러그>.md`
- `순서` 번호는 분류별 100단위: 운영 100, 청소 200, 기본 300, 칵테일 400,
  와인 500, 안주 600, 음악 700, 마감 800
  - 이 번호가 **탭 순서와 항목 순서를 동시에 결정**한다. 임의로 바꾸지 않는다
- 프런트매터 키는 한국어(`제목`)와 영어(`title`) 둘 다 인식한다. 폴더 언어에 맞춰 쓴다
- `하위분류` / `group` 으로 분류 안에서 한 단계 더 나눌 수 있다. 같은 분류에 둘 이상
  있으면 하위 탭이 자동으로 생기고, 하나 이하면 그리지 않는다. 항목이 열 개를 넘어가는
  분류에는 넣어 주는 편이 낫다 (예: 칵테일 → 기본기 / 레시피)
- **하위분류 이름도 언어별로 번역한다** (기본기 ↔ Fundamentals, 레시피 ↔ Recipes)
- 미완성 항목은 `공개: false` / `published: false` 로 숨긴다

## 레시피

- 칵테일 한 종류 = 파일 하나. 순서 번호 410번대부터
- **레시피는 사장님이 준 스펙 그대로 적는다.** 표준 레시피로 "고쳐" 쓰지 않는다
- 약칭은 풀어서 적는다 (메막 → 메이커스 마크, 탱텐 → 탱커레이 넘버텐).
  다만 확신이 없는 약칭은 임의로 단정하지 말고 확인을 요청한다
- 방식이 여러 개인 레시피는 번호를 붙여 모두 남긴다. 하나를 고르지 않는다
- 단위·얼음 크기·기법 용어는 "레시피 읽는 법"(409) 항목에 모아둔다
- **가격은 적지 않는다.** 메뉴판이 따로 있고, 바뀔 때마다 이 문서가 틀려진다

## 매장 고유 정보는 지어내지 않는다

메뉴 구성, 오픈·마감 루틴, 쓰레기 배출 장소, 장비 조작법, 시그니처 레시피는
**추측해서 쓰지 않는다.** 모르면 뼈대만 만들고 `공개: false` 로 두되, 무엇을 알아야
하는지 질문 목록을 파일 안에 적어둔다.

일반적인 바텐딩 지식(파지법, 잔 이름, 용어, 클래식 표준 레시피, 위생)은 작성해도 된다.
단, 표준 스펙임을 밝히고 매장 스펙이 다를 수 있음을 명시한다.

## 기술 제약

- **React·Tailwind를 쓰지 않는다.** 손님용 사이트(`otg-bar`)와 같은 계열을 유지한다:
  TypeScript + Vite, 바닐라 DOM, 직접 쓴 CSS
- 런타임 의존성을 늘리지 않는다 (현재 0개)
- **영상 파일과 큰 사진을 레포에 커밋하지 않는다.** git은 모든 버전을 영구 보관하므로
  레포가 영영 무거워진다. 영상은 유튜브에 두고 `{제공자, 영상ID}` 로만 참조한다
- 콘텐츠는 서버 함수를 거쳐 나간다. 정적 번들에 넣지 않는다 — 비밀번호가 무의미해진다

## 작업 후

`npm run build` 로 타입 체크와 빌드를 확인한 뒤 커밋한다.
