# 처음 세팅하기 (노션을 한 번도 안 써봤어도 됩니다)

**중요: 이 문서의 과정은 딱 한 번만 합니다.** 이게 끝나면 이후로는 노션 앱에서 표에 줄 추가하고 글 쓰는 것뿐입니다.

**컴퓨터에서 하세요.** 폰으로도 되지만 복사·붙여넣기가 많아서 컴퓨터가 훨씬 편합니다. 세팅만 컴퓨터로 하고, 그 뒤 실제 사용은 폰에서 합니다.

전체 소요: 20~30분. 막히면 그 단계 번호를 말씀해 주세요.

---

## 1단계 — 노션 계정 만들기 (3분)

1. [notion.com](https://www.notion.com) 접속 → 우측 상단 **무료로 시작하기**
2. 구글 계정으로 가입하면 제일 빠릅니다.
3. 가입 중에 "어떤 용도로 쓰나요?" 같은 질문이 나오면 아무거나 골라도 됩니다. 나중에 영향 없습니다.
4. 가입이 끝나면 왼쪽에 목록, 오른쪽에 넓은 빈 공간이 있는 화면이 뜹니다. 왼쪽 목록이 **사이드바**, 오른쪽이 **페이지**입니다. 이 두 단어만 알면 됩니다.

## 2단계 — 가이드 표 만들기 (5분)

1. 사이드바에서 **+ 새 페이지**(또는 `+`)를 누릅니다.
2. 페이지 맨 위 제목 자리에 `OTG 가이드` 라고 씁니다.
3. 제목 아래 본문을 한 번 클릭하고 `/표` 라고 칩니다. 메뉴가 뜨면 **표**(데이터베이스 - 인라인)를 고릅니다.
   - 슬래시(`/`)를 치면 메뉴가 나오는 게 노션의 기본 조작법입니다. 이거 하나면 됩니다.
4. 엑셀 비슷한 표가 생깁니다. 기본으로 `이름`, `태그` 같은 열이 있습니다.
5. **열 이름을 아래와 똑같이 만듭니다.** 이름이 한 글자라도 다르면 사이트가 못 읽습니다.
   열 이름을 클릭하면 이름과 종류를 바꿀 수 있고, 맨 오른쪽 `+`로 열을 추가합니다.

   | 열 이름 | 종류 | 무엇을 적나 |
   |---|---|---|
   | `제목` | 제목 | `네그로니`, `오픈 준비` 같은 항목 이름 |
   | `분류` | 선택 | `칵테일` 또는 `운영`. 사이트 상단 탭이 됩니다 |
   | `요약` | 텍스트 | 목록에 한 줄로 보일 설명 |
   | `순서` | 숫자 | 작을수록 위로. 비우면 맨 아래 |
   | `영상` | URL | 유튜브 링크를 그대로 붙여넣습니다 |
   | `공개` | 체크박스 | **체크해야 사이트에 보입니다.** 작성 중인 건 체크 해제 |

   - 첫 번째 열(`이름`)은 종류를 못 바꿉니다. 이름만 `제목`으로 고치면 됩니다.
   - 안 쓰는 기본 열(`태그` 등)은 열 이름 클릭 → **속성 삭제**로 지웁니다.

6. 시험 삼아 한 줄 채워봅니다. `제목`에 `네그로니`, `분류`에 `칵테일`, `공개` 체크.
7. 그 줄 왼쪽 끝에 마우스를 올리면 **열기**(또는 `↗`) 버튼이 나옵니다. 누르면 그 항목의 **본문**이 열립니다.
   여기에 실제 레시피를 씁니다. 사진은 그냥 끌어다 놓거나 붙여넣으면 됩니다.
   사이트는 본문의 제목/문단/글머리 목록/번호 목록/사진을 그대로 가져갑니다.

## 3단계 — 사이트가 노션을 읽을 수 있게 연결 (10분)

사이트가 사장님 노션을 대신 읽으려면 **열쇠(토큰)** 가 필요합니다. 아래가 그 열쇠를 만드는 과정입니다.

1. 노션 좌측 하단(또는 우측 상단 계정 메뉴) → **설정**으로 들어갑니다.
2. 설정 안에서 **개발자 모드**(Developer mode)를 켭니다. 이걸 켜야 다음 메뉴가 보입니다.
3. 사이드바에 생긴 개발자 항목에서 **+ 새 연결**(New connection)을 누릅니다.
4. 입력할 것:
   - **이름**: `OTG 가이드 사이트` (아무거나 상관없음)
   - **연결된 워크스페이스**: 방금 만든 본인 워크스페이스
   - **기능(capabilities)**: **콘텐츠 읽기(Read content)** 만 체크하면 됩니다. 쓰기 권한은 필요 없습니다.
5. **제출**을 누르면 다음 화면에 긴 문자열이 나옵니다. `ntn_` 또는 `secret_` 로 시작합니다.
   → **이게 `NOTION_TOKEN` 입니다. 복사해서 메모장에 붙여두세요.**
   - 이건 비밀번호나 마찬가지입니다. 카톡이나 메일로 남에게 보내지 마세요.
6. **2단계에서 만든 `OTG 가이드` 페이지로 돌아갑니다.**
7. 페이지 우측 상단 `···`(점 세 개) → **연결 추가**(Connect to) → 방금 만든 `OTG 가이드 사이트` 선택.
   - **이 단계를 빠뜨리면 반드시 권한 오류가 납니다.** 가장 흔한 실수입니다.
8. 이제 주소창의 URL을 봅니다. 이렇게 생겼습니다:

   ```
   https://www.notion.so/내워크스페이스/1a2b3c4d5e6f7890abcdef1234567890?v=...
                                        └─────── 이 32자리가 ID ───────┘
   ```

   → **이게 `NOTION_DATABASE_ID` 입니다.** `?v=` 앞까지, 맨 뒤 32자리만 복사합니다.
   - 표를 **인라인**으로 만들었다면, 표 우측 상단 `···` → **전체 페이지로 열기**를 한 뒤 URL을 보는 게 정확합니다.

## 4단계 — Vercel에 올리기 (10분)

1. [vercel.com](https://vercel.com) 접속 → **GitHub 계정으로 가입**.
2. **Add New → Project** → 목록에서 `otg-bar-guide` 레포를 고르고 **Import**.
3. 배포 설정은 건드리지 않아도 됩니다 (`vercel.json`에 다 적혀 있습니다).
4. **Environment Variables**에 4개를 넣습니다:

   | 이름 | 값 |
   |---|---|
   | `SITE_PASSWORD` | 직원들에게 알려줄 사이트 비밀번호 (직접 정하세요) |
   | `SESSION_SECRET` | 아무 길고 복잡한 문자열. 키보드 마구 쳐도 됩니다 |
   | `NOTION_TOKEN` | 3단계 5번에서 복사한 열쇠 |
   | `NOTION_DATABASE_ID` | 3단계 8번에서 복사한 32자리 |

5. **Deploy**를 누르고 1~2분 기다립니다.
6. 나온 주소를 폰으로 열어 비밀번호를 넣어봅니다.

**노션 연동을 아직 안 했어도 3·4번 환경변수를 비워두고 먼저 배포할 수 있습니다.** 그러면 예시 콘텐츠(네그로니 / 오픈 준비)로 화면이 뜹니다. 모양부터 보고 싶으시면 이 방법이 제일 빠릅니다.

---

## 이후 일상 사용

폰에 **노션 앱**을 깔고 로그인합니다. `OTG 가이드` 페이지를 즐겨찾기 해두세요.

새 레시피를 올릴 때:
1. 표에서 `+ 새로 만들기`
2. `제목`, `분류`, `요약`, `순서` 채우기
3. 항목을 열어 본문에 만드는 법 쓰고 사진 첨부
4. `공개` 체크

끝입니다. 사이트에 1분 안에 반영됩니다. **재배포도, 개발자 연락도 필요 없습니다.**

## 자주 나는 오류

| 증상 | 원인 |
|---|---|
| 사이트에 아무것도 안 뜸 | `공개` 체크박스를 안 했거나, 3단계 7번(연결 추가)을 빠뜨림 |
| 예시 콘텐츠(네그로니)만 뜸 | Vercel 환경변수에 `NOTION_TOKEN` / `NOTION_DATABASE_ID`가 비어 있음 |
| 일부 항목만 안 보임 | 그 항목의 `공개` 체크 누락, 또는 `제목`이 비어 있음 |
| 사진이 안 올라감 | 노션 무료 플랜은 파일 1개당 5MB 제한. 아이폰은 `설정 → 카메라 → 포맷 → 고효율` |
| 탭이 이상하게 나뉨 | `분류` 값의 오타. `칵테일`과 `칵테일 ` (뒤 공백)은 다른 탭이 됩니다 |

> 노션 화면은 수시로 바뀝니다. 메뉴 이름이 위와 다르면 그 단계 번호와 보이는 화면을 말씀해 주세요.
