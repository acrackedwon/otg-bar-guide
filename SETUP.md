# 세팅과 사용법

## 딱 한 번 — 배포 (10분)

1. [vercel.com](https://vercel.com) → **GitHub 계정으로 가입**
2. **Add New → Project** → `otg-bar-guide` 레포 선택 → **Import**
3. 빌드 설정은 건드리지 않습니다 (`vercel.json`에 다 적혀 있습니다)
4. **Environment Variables**에 2개를 넣습니다:

   | 이름 | 값 |
   |---|---|
   | `SITE_PASSWORD` | 직원들에게 알려줄 사이트 비밀번호 (직접 정하세요) |
   | `SESSION_SECRET` | 아무 길고 복잡한 문자열. 키보드 마구 쳐도 됩니다 |

5. **Deploy** → 1~2분 뒤 나온 주소를 폰으로 열어 비밀번호를 넣어봅니다.

예시 콘텐츠(네그로니 / 오픈 준비)가 이미 들어 있어서 바로 화면이 뜹니다.

---

## 평소 — 내용 추가·수정

**클로드한테 말하면 됩니다.** 폰에서 세션 열고 그냥 말로 하세요.
**한국어만 말씀하셔도 영어판이 같이 만들어집니다** — 규칙으로 정해져 있습니다.

> "마티니 추가해줘. 진 60, 드라이 베르무트 10, 스터 30초, 올리브."
>
> "네그로니 레시피에서 스터 시간 20초를 30초로 고쳐줘."
>
> "마감 절차 항목 만들어줘. 내용은 나중에 채울 거니까 일단 숨겨놔."
>
> "이 유튜브 링크를 셰이킹 항목 영상으로 걸어줘. https://youtu.be/..."
>
> "칵테일 탭에서 네그로니가 마티니보다 위에 오게 해줘."

형식·파일 이름·순서 같은 건 신경 쓰지 않으셔도 됩니다. 커밋하고 푸시하면
**1~2분 뒤 사이트에 자동 반영**됩니다.

### 영상 올릴 때

1. 유튜브에 올립니다 (폰 유튜브 앱에서 바로 가능)
2. 공개 범위를 **일부 공개(Unlisted)** 로 — 링크를 아는 사람만 봅니다
3. **공유 → 링크 복사** 해서 클로드한테 주면서 어느 항목에 걸지 말씀하세요

촬영은 **30초~1분 단위로 짧게, 1080p** 로. 4K로 길게 찍으면 업로드가 오래 걸리고 편집이 번거롭습니다.

---

## 참고 — 직접 고치고 싶을 때

클로드 없이 급하게 고쳐야 하면, 폰 브라우저에서도 됩니다.

[github.com/acrackedwon/otg-bar-guide](https://github.com/acrackedwon/otg-bar-guide) → `content` 폴더 → 파일 선택 → **연필 아이콘(✏️)** → 고치고 → 맨 아래 **Commit changes**

콘텐츠는 `content/ko`(한국어)와 `content/en`(영어) 폴더에 **파일 하나 = 항목 하나**로 들어 있습니다.
**같은 이름의 파일끼리 짝**이고, 한쪽만 고치면 다른 언어는 옛날 내용 그대로 남으니 주의하세요.
생김새는 이렇습니다:

```
---
제목: 마티니
분류: 칵테일
요약: 진 60 / 드라이 베르무트 10, 스터, 올리브.
하위분류: 레시피
순서: 2
영상: https://youtu.be/영상주소
공개: true
---

## 레시피

- 진 60ml
- 드라이 베르무트 10ml

## 만드는 법

1. 믹싱 글라스와 잔을 미리 차갑게 해둔다.
2. 재료를 넣고 30초간 스터한다.
```

| 칸 | 설명 |
|---|---|
| `제목` | 항목 이름. 비우면 사이트에 안 나옵니다 |
| `분류` | 사이트 상단 **탭**이 됩니다 |
| `하위분류` | 탭 안에서 한 번 더 나누고 싶을 때. 없으면 생략 |
| `요약` | 목록에 한 줄로 보이는 설명 |
| `순서` | 숫자. 작을수록 위. 비우면 맨 아래 |
| `영상` | 유튜브 주소 그대로. 없으면 비워둠 |
| `공개` | `false`면 숨겨집니다 (작성 중인 글) |

영어 파일(`content/en`)은 키 이름이 영어입니다: `title`, `category`, `group`, `summary`, `order`, `video`, `published`.

본문 기호는 4개가 전부입니다: `##` 소제목, `-` 목록, `1.` 순서, 나머지는 그냥 글.
사진은 `![설명](사진주소)`.

고쳤는데 그대로면 배포에 1~2분 걸리는 것이니 잠시 뒤 새로고침해 보세요.

---

## 언어

사이트 우측 상단 **KO / EN** 버튼으로 전환합니다. 고른 언어는 그 폰에 기억되므로,
외국인 직원은 한 번만 EN을 누르면 이후로 계속 영어로 열립니다.

나중에 일본어·중국어를 추가하고 싶으시면 말씀만 하세요. 구조는 이미 잡혀 있습니다.
