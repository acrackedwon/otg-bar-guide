# 처음 세팅하기

**한 번만 하면 됩니다. 10분.** 노션도, 토큰도, 가입할 서비스도 없습니다.

## 1단계 — Vercel에 올리기

1. [vercel.com](https://vercel.com) → **GitHub 계정으로 가입**
2. **Add New → Project** → `otg-bar-guide` 레포 선택 → **Import**
3. 빌드 설정은 건드리지 않습니다 (`vercel.json`에 다 적혀 있습니다)
4. **Environment Variables**에 2개를 넣습니다:

   | 이름 | 값 |
   |---|---|
   | `SITE_PASSWORD` | 직원들에게 알려줄 사이트 비밀번호 (직접 정하세요) |
   | `SESSION_SECRET` | 아무 길고 복잡한 문자열. 키보드 마구 쳐도 됩니다 |

5. **Deploy** → 1~2분 뒤 나온 주소를 폰으로 열어 비밀번호를 넣어봅니다.

끝입니다. 예시 콘텐츠(네그로니 / 오픈 준비)가 이미 들어 있어서 바로 화면이 뜹니다.

---

# 내용 추가·수정하기 (폰에서)

콘텐츠는 레포의 `content` 폴더 안에 **글 파일 하나 = 항목 하나** 로 들어 있습니다.
폰 브라우저에서 GitHub에 들어가 고치면 **1~2분 뒤 사이트에 자동 반영**됩니다.

## 기존 내용 고치기

1. 폰 브라우저로 [github.com/acrackedwon/otg-bar-guide](https://github.com/acrackedwon/otg-bar-guide) 접속 → 로그인
2. `content` 폴더 → 고칠 파일 선택 (예: `negroni.md`)
3. 우측 상단 **연필 아이콘(✏️)** 누르기
4. 내용 고치기
5. 맨 아래 **Commit changes** 누르기

## 새 항목 추가하기

1. `content` 폴더로 들어가서 우측 상단 **Add file → Create new file**
2. 파일 이름을 영문으로 짓습니다. **반드시 `.md`로 끝나야 합니다.** 예: `martini.md`, `closing.md`
3. 아래 형식을 그대로 복사해서 붙여넣고 내용만 바꿉니다
4. **Commit changes**

```
---
제목: 마티니
분류: 칵테일
요약: 진 60 / 드라이 베르무트 10, 스터, 올리브 또는 레몬 필.
순서: 2
영상: https://youtu.be/영상주소
공개: true
---

## 레시피

- 진 60ml
- 드라이 베르무트 10ml

## 만드는 법

1. 믹싱 글라스와 잔을 미리 차갑게 해둔다.
2. 재료를 넣고 30초간 스터한다.
3. 차가운 칵테일 잔에 따르고 올리브를 넣는다.
```

## 맨 위 칸(`---` 사이) 쓰는 법

| 칸 | 설명 |
|---|---|
| `제목` | 항목 이름. **비우면 사이트에 안 나옵니다** |
| `분류` | 사이트 상단 **탭**이 됩니다. `칵테일`, `운영` 등 |
| `요약` | 목록에 한 줄로 보이는 설명 |
| `순서` | 숫자. 작을수록 위에 표시. 비우면 맨 아래 |
| `영상` | 유튜브 주소를 그대로 붙여넣기. 없으면 비워둠 |
| `공개` | `true`면 보이고, `false`면 숨겨집니다. 작성 중인 글은 `false` |

## 본문 쓰는 법 (기호 4개가 전부)

| 쓰고 싶은 것 | 이렇게 씁니다 |
|---|---|
| 소제목 | `## 만드는 법` (`##` 뒤에 한 칸 띄우기) |
| 재료 목록 | `- 진 30ml` (`-` 뒤에 한 칸 띄우기) |
| 순서 있는 단계 | `1. 얼음을 채운다` (숫자, 점, 한 칸) |
| 그냥 설명 문단 | 아무 기호 없이 그대로 씁니다 |
| 사진 | `![설명](사진주소)` — 아래 참고 |

그 외의 기호(`**굵게**` 등)는 해석하지 않고 글자 그대로 나옵니다.

## 영상 넣기

1. 유튜브에 영상을 올립니다 (폰 유튜브 앱에서 바로 가능)
2. 공개 범위를 **일부 공개(Unlisted)** 로 — 링크를 아는 사람만 봅니다
3. 그 영상의 **공유 → 링크 복사**
4. 글 맨 위 `영상:` 칸에 붙여넣기

촬영은 **30초~1분 단위로 짧게, 1080p** 로. 4K로 길게 찍으면 업로드가 오래 걸리고 편집이 번거롭습니다.

## 사진 넣기

지금은 사진 주소(URL)를 본문에 적는 방식입니다:

```
![셰이킹 자세](https://사진주소.jpg)
```

폰에서 찍은 사진을 바로 올리고 싶으시면 말씀해 주세요. GitHub에 직접 올리는 방법을 안내하거나, 사진 업로드를 편하게 만들어 드리겠습니다.

## 자주 나는 문제

| 증상 | 원인 |
|---|---|
| 새로 쓴 글이 안 보임 | `공개: false` 로 되어 있거나 `제목` 이 비었음 |
| 파일이 통째로 안 읽힘 | 맨 위 `---` 두 줄이 빠졌거나 파일 이름이 `.md`로 안 끝남 |
| 목록이 한 줄로 붙어 나옴 | `-` 나 `1.` 뒤에 **한 칸 띄우기**를 안 함 |
| 탭이 이상하게 나뉨 | `분류` 오타. `칵테일`과 `칵테일 `(뒤 공백)은 다른 탭이 됩니다 |
| 고쳤는데 그대로임 | 배포에 1~2분 걸립니다. 잠시 뒤 새로고침 |

---

> 나중에 글이 많아져서 폰에서 GitHub를 거치는 게 번거로워지면, 그때 노션 연동이나 사이트 내 업로드로 바꿀 수 있습니다. 화면과 나머지 구조는 그대로 두고 읽어오는 곳만 바꾸면 됩니다 (`lib/markdown.ts` 한 파일).
